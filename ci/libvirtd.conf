# Isolated CI daemon. Never use this configuration on a production host.
listen_tls = 0
listen_tcp = 1
tcp_port = "16509"
listen_addr = "127.0.0.1"
auth_tcp = "none"
auth_unix_rw = "none"
log_level = 1
log_outputs = "1:file:/tmp/libvirtd-ci.log"
